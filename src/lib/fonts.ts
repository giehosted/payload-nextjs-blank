import { Inter, Satisfy } from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
})

export const satisfy = Satisfy({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
})
